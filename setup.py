from setuptools import setup, find_packages

setup(
    name = 'simple_rl',
    version = '0.0.0',
    author = '',
    author_email = '',
    description = '',
    packages = find_packages(),
    install_requires = []
)
